# predict-api/requirements.txt
# NOTE: dependencies inlined from project root requirements.txt
# (do NOT use "-r ../requirements.txt" : the parent file is outside Docker build context)

# === Core stack ===
python-dotenv==1.0.1
requests==2.32.3
pydantic==2.7.4
fastapi==0.111.0
uvicorn[standard]==0.30.1

# === Scientific stack ===
numpy==1.26.4
scipy==1.12.0
pandas==2.2.2
scikit-learn==1.6.0

# === Image + NLP ===
opencv-python-headless==4.10.0.84
pillow==10.4.0

# === Deep learning (CPU safe) ===
tensorflow-cpu==2.17.0

# === MLflow + DagsHub ===
mlflow==2.17.2
boto3==1.35.76
dagshub==0.7.0

# === Monitoring ===
prometheus-client==0.20.0
prometheus-fastapi-instrumentator

# === Text encoders ===
sentence-transformers>=2.7.0

