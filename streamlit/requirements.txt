# streamlit/requirements.txt
# Standalone — does NOT use -r ../requirements.txt.
# The streamlit app calls predict-api via HTTP and renders results;
# it never loads ML models, so torch/tensorflow/spacy are not needed.

requests==2.32.3
pandas==2.2.2
openpyxl>=3.1.0
mlflow==2.8.1
streamlit==1.36.0
plotly==5.22.0
altair==5.3.0
